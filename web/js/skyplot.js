// web/js/skyplot.js — placeholder; real implementation lands in Task 7.
export class Skyplot {
  constructor(elId, store) {
    this.elId = elId;
    this.store = store;
  }
  render() {
    // no-op until Task 7
  }
}
