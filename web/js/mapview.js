// web/js/mapview.js — placeholder; real implementation lands in Task 7.
export class MapView {
  constructor(elId, store) {
    this.elId = elId;
    this.store = store;
  }
  render() {
    // no-op until Task 7
  }
}
